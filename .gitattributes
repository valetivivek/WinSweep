# Normalize line endings: store as LF in the repo, check out native locally.
* text=auto eol=lf

# Treat binary assets as binary so they are never line-ending normalized.
*.png binary
*.ico binary
*.icns binary
*.woff binary
*.woff2 binary
*.ttf binary
